--- src/core/tak_encoder_subframe.cpp
+++ src/core/tak_encoder_subframe.cpp
@@ -58,3 +58,4 @@
         }
-        fw.write_bit(1);
+        fw.write_bit(0); // single segment
+        fw.write_bit(1); // new mode
         fw.write_bits(best_m, 6);
