--- src/core/tak_encoder_entropy.cpp
+++ src/core/tak_encoder_entropy.cpp
@@ -165,15 +165,11 @@
                 } else {
-                    int scale_bits = 0;
-                    uint32_t s_copy = scale;
+                    int scale_bits = 1;
+                    uint32_t s_copy = scale - 1;
                     while (s_copy > 0) {
                         scale_bits++;
                         s_copy >>= 1;
                     }
+                    scale_bits--;
+                    if (scale_bits == 0) scale_bits = 1;
 
                     if (scale_bits <= 6) {
                         bw.write_bits(scale_bits, 3);
-                        bw.write_bits(scale - (1U << (scale_bits - 1)), scale_bits - 1);
+                        bw.write_bits(scale - 1, scale_bits);
                     } else if (scale_bits <= 29) {
                         bw.write_bits(7, 3);
                         bw.write_bits(scale_bits, 5);
-                        bw.write_bits(scale - (1U << (scale_bits - 1)), scale_bits - 1);
+                        bw.write_bits(scale - 1, scale_bits);
                     } else {
@@ -213,15 +209,11 @@
                 } else {
-                    int scale_bits = 0;
-                    uint32_t s_copy = scale;
+                    int scale_bits = 1;
+                    uint32_t s_copy = scale - 1;
                     while (s_copy > 0) {
                         scale_bits++;
                         s_copy >>= 1;
                     }
+                    scale_bits--;
+                    if (scale_bits == 0) scale_bits = 1;
 
                     if (scale_bits <= 6) {
                         bits += 3;
-                        bits += scale_bits - 1;
+                        bits += scale_bits;
                     } else if (scale_bits <= 29) {
                         bits += 3 + 5;
-                        bits += scale_bits - 1;
+                        bits += scale_bits;
                     } else {
